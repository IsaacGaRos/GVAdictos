"""PostgreSQL migration helper for F4.

Migration is handled by Alembic. See:
  - alembic/env.py — migration configuration
  - alembic/versions/ — migration scripts
  - src/db/database.py — connection factory
  - src/db/models.py — ORM models
"""
